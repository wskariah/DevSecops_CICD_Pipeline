# SonarQube Project Properties
sonar.projectKey=hello-world-app
sonar.projectName=Hello World App
sonar.projectVersion=1.0.${BUILD_NUMBER}  # Use Jenkins' BUILD_NUMBER to version your code
sonar.sources=src  # The directory containing your source code
sonar.language=java  # Language of your project (can be omitted if sonar.language is automatically detected)
sonar.java.binaries=target/classes  # Where your compiled classes reside
sonar.java.source=17  # Java version of the source code (adjust to your app's JDK version)
