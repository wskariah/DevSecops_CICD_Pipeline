resources:
- ../../base

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
patches:
- path: patch-deployment.yaml

labels:
- includeSelectors: true
  pairs:
    project: t3aCarbon-PaC

# Add common annotations
commonAnnotations:
  sonarqube-status: "success"  # This will be overwritten by the pipeline